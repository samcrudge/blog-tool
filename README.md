# blog-tool

Simple blog tool that manages posts.
PR'd by peers.

Once cloned run
``` Composer install ```

then run
```Composer start```

Tests are being added later.

To run these use 
``` composer test```
